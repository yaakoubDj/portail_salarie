import 'package:flutter/material.dart';

class GBSystemDocumentScreen extends StatelessWidget {
  const GBSystemDocumentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("documents"),
    );
  }
}
